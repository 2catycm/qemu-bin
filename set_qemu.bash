qemu-as-7.0-4Ki(){
  echo "正在设置Qemu为 7.0-4Ki"
  echo "当前脚本位置${BASH_SOURCE[0]}"
  export PATH=$PATH:${BASH_SOURCE[0]}/qemu-7.0-4Ki
  echo "设置完成"
}
qemu-as-7.0-16Ki(){
  echo "正在设置Qemu为 7.0-16Ki"
  echo "当前脚本位置${BASH_SOURCE[0]}"
  export PATH=$PATH:${BASH_SOURCE[0]}/qemu-7.0-16Ki
  echo "设置完成"
}

